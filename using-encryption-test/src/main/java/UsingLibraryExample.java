import net.veritran.encryption.providerrrr.Provider;

public class UsingLibraryExample {
    Provider provider = new Provider();
    public void encryptMessage(){
        provider.encryptMessage("","","","");
    }
}